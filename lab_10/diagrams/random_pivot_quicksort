@startuml

start

:Choose pivot element randomly;
:Swap pivot with the last element;
:Partition the array around the pivot using standard partition function <font color=red> O(n) </font>;
:Recursively apply quicksort to the left partition <font color=red> T(k) </font>;
:Recursively apply quicksort to the right partition <font color=red> T(n-k-1) </font>;
stop

@enduml
